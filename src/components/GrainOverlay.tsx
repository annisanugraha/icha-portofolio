'use client';

/**
 * GrainOverlay — Cinematic film grain texture
 * Fixed position overlay that adds an analog, premium feel to the entire page.
 * Uses SVG noise filter rendered as a CSS background, very lightweight.
 */
export function GrainOverlay() {
  return <div className="grain-overlay" aria-hidden="true" />;
}
